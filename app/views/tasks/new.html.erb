<div class="container">
  <%= simple_form_for [@goal, @task] do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.button :submit, class: 'btn btn-success' %>
  <% end %>
</div>
