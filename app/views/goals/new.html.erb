<div class="goal-container">
  <%= simple_form_for @goal do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :category, collection: Goal::CATEGORIES.map { |category| category[:name] } %>
    <%= f.input :start_date %>
    <%= f.input :end_date %>
    <%= f.button :submit, class: 'btn-primary-green' %>
  <% end %>
</div>
